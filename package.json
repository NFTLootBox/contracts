{
  "name": "NFTLootBoxContracts",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.0",
    "@nomiclabs/hardhat-waffle": "^2.0.0",
    "chai": "^4.2.0",
    "ethereum-waffle": "^3.0.0",
    "ethers": "^5.0.24",
    "hardhat": "^2.0.5"
  },
  "scripts": {
    "test": "hardhat test",
    "compile": "hardhat compile",
    "network": "npx hardhat node --fork $ALCHEMY_PROVIDER",
    "deploy": "npx hardhat run scripts/deploy.js --network rinkeby",
    "deploySwap": "npx hardhat run scripts/deploySwap.js --network rinkeby",
    "oofta": "npx hardhat run scripts/testing.js --network mainnet"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^3.3.0",
    "bignumber.js": "^9.0.1",
    "hardhat-abi-exporter": "^2.0.8"
  }
}
